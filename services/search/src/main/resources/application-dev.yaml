spring:
  application:
    name: "QuickstartSearchService"
  config:
    activate.on-profile: dev
  kafka:
    bootstrap-servers: ${BOOTSTRAP_SERVER}
    properties:
      schema.registry.url: ${SR_URL}
      basic.auth.credentials.source: USER_INFO
      schema.registry.basic.auth.user.info: ${SR_API_KEY}:${SR_API_SECRET}
      security.protocol: SASL_SSL
      sasl.mechanism: PLAIN
      sasl.jaas.config: org.apache.kafka.common.security.plain.PlainLoginModule required username="${KAFKA_API_KEY}" password="${KAFKA_API_SECRET}";
      max.poll.interval.ms: 1000
    streams:
      client-id: ${CLIENT_ID}|search
      application-id: ${CLIENT_ID}_search_app
      properties:
        default-key-serde: io.confluent.pie.quickstart.gcp.mongodb.entities.ChatKeySerde
        default-value-serde: io.confluent.pie.quickstart.gcp.mongodb.entities.ChatInputSerde
mongodb:
  uri: mongodb+srv://${MONGODB_USER}:${MONGODB_PWD}@${MONGODB_HOST}/?retryWrites=true&w=majority
  database: ${MONGODB_DATABASE}
  collection: ${MONGODB_COLLECTION}
  field_path: "embeddings"
  index_name: "products_summarized_with_embeddings-vector"
server:
  port: ${PORT:8081}
